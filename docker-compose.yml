version: '3.9'

services:
  baza-back:
    env_file:
      - .env
    build: .
    container_name: baza-back
    restart: unless-stopped
    ports:
      - ${PORT}:${PORT}
    volumes:
      - baza-static:/uploads
    networks:
      - mongodb
      - localnet

networks:
  mongodb:
    external: true
    name: mongodb

  localnet:
    external: true
    name: localnet

volumes:
  baza-static:
